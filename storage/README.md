# Storage Layer

This folder contains the data storage implementation for action items and meetings.

## Contents

- `models.py` — Data model schema (action items, meetings)
- `storage.py` — Storage implementation (SQLite or JSON)
- `migrations/` — Database schema updates (if needed)

## Usage

Simple interface for creating, reading, and updating action items.

See `../requirements.md` for the full schema specification.
